# Copy to .env and fill in to use the optional Python ad/video generation
# scripts (generate-ads*.py, generate-*-videos*.py, recaption*.py, etc.).
# The 360 tour site itself (index.html) needs no keys — just a static server.

# Google Gemini API key used by the image/video generation scripts.
GEMINI_API_KEY=
